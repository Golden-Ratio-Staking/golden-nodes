server {

        server_name rpc-<chain>.goldenratiostaking.net;

        location / {
                proxy_pass http://rpc-<chain>.goldenratiostaking.net:<RPC Port>/;
        server_name_in_redirect off;
        proxy_set_header Host $host;
        }
